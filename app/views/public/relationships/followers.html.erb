<% if @users.exists? %>
<div class="container">
 <div class="text-center m-4">
  <% @users.each do |user| %>
    <% if user.image.attached? %>
     <%= image_tag user.image, size: "100x100" %>
    <% else %>
     <%= image_tag 'logo2', size: "100x100", class: "rounded mx-auto mb-2" %>
    <% end %>
      <h5>ユーザー名</h6>
      <%= user.user_name %>
  <% end %>
 </div>
</div>
<% else %>
  <p>ユーザーはいません</p>
<% end %>
