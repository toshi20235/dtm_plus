<div class="container">
   <div class="row">
     <div>
       <%= image_tag user.get_image(100,100) %>
       <%= user.user_name %>
     </div>
     <div>
       <%= audio_tag t.audio_url, :controls => true if t.audio? %>
     </div>